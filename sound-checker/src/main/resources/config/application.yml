spring:
  main:
    banner-mode: off
  aop:
    proxy-target-class: false
---
logging:
  charset:
    console: ${native.encoding:${file.encoding}}
  file:
    name: ${user.home}/logs/sound-checker/error.log
  level:
    root: WARN
    jp.gr.java_conf.stardiopside.sound: INFO
  threshold:
    file: ERROR
